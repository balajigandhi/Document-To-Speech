# Context Export — 2026-07-10 17:57:01

## Session Summary

Set up the Document-To-Speech project (a Mozilla.ai Blueprint fork by alexmeckes) on a Mac with Python 3.14 (incompatible), installed Python 3.12 via Homebrew, created a venv, and installed all dependencies. Fixed four bugs found during live testing of the Streamlit app. Evaluated the repo against mozilla-ai/document-to-podcast and decided to stick with alexmeckes for its single-voice, no-fluff output. Discussed adding a FastAPI sidecar + Tampermonkey script to enable TTS directly from wiki pages.

## Current State

- **Done:** Project installed and running at http://localhost:8501
- **Done:** Four bugs fixed (see Files Changed)
- **In progress:** User plans to fork the repo to save changes
- **Not started:** FastAPI sidecar endpoint for Tampermonkey integration

## Key Decisions Made

- Use Python 3.12 (system has 3.14, project requires <3.13) — installed via Homebrew
- Stick with alexmeckes/Document-To-Speech over mozilla-ai/document-to-podcast — user wants straight wiki reading, no podcast dialogue fluff
- FastAPI sidecar (Option C) chosen for Tampermonkey integration: Tampermonkey extracts page text, POSTs to localhost:8080/tts, gets WAV back, plays inline
- WAV output should use PCM_16 subtype for QuickTime/macOS compatibility
- URL loader needs browser User-Agent to avoid bot-blocking on most sites; authenticated pages (e.g. internal wikis) still won't work via URL — user must download as HTML and upload

## Files Changed

- `src/document_to_speech/preprocessing/__init__.py` — added `"url": clean_html` to `DATA_CLEANERS` dict (was missing, caused KeyError)
- `src/document_to_speech/config.py` — changed `openrouter_model: str` to `openrouter_model: Optional[str]` (pydantic ValidationError when local LLM selected and field passed None)
- `demo/app.py` — added `subtype="PCM_16"` to `sf.write()` call in `numpy_to_wav()` (QuickTime error -12842 on downloaded WAV)
- `src/document_to_speech/preprocessing/data_loaders.py` — added browser User-Agent header and 15s timeout to `load_url()` (sites returning bot-blocked minimal HTML with no body content)

## Conversation History

**Setup:** User said "Setup this project." Working directory was `/Users/bgandhi/Projects/Hackday/text-to-speech`. Found one subdirectory: `Document-To-Speech`. Read README and pyproject.toml — Python 3.10–3.12 required, main deps are kokoro, llama-cpp-python, streamlit. System had Python 3.14 only. Installed python@3.12 via Homebrew, created venv, ran `pip install -e .` — succeeded with all deps including llama-cpp-python (compiled from source) and kokoro.

**Log review:** User shared path to `logs/app.log`. Log showed server running fine on port 8501. Bulk of log was harmless `ModuleNotFoundError: No module named 'torchvision'` from Streamlit's file watcher trying to inspect transformers image-model submodules. Explained these are irrelevant — project is text-only TTS, torchvision never used at runtime.

**Bug 1 — KeyError 'url':** App crashed at `DATA_CLEANERS[extension](raw_text)` when extension was `"url"`. `DATA_LOADERS` had `"url"` key, `DATA_CLEANERS` did not. Fixed by adding `"url": clean_html` to `DATA_CLEANERS` in `__init__.py`.

**Bug 2 — Pydantic ValidationError:** `Config` model crashed with `Input should be a valid string [type=string_type, input_value=None]` for `openrouter_model`. App passes `None` for that field when local LLM is selected. `config.py` had `openrouter_model: str` with no Optional. Fixed to `openrouter_model: Optional[str]`.

**Bug 3 — QuickTime error -12842:** User shared screenshot of macOS dialog "The document 'speech.wav' could not be opened. An unknown error occurred (-12842)." Error means QuickTime can't play the audio format. `sf.write` defaults to float64 WAV which macOS QuickTime rejects. Fixed by adding `subtype="PCM_16"` to the soundfile write call in `numpy_to_wav()`.

**Bug 4 — Empty cleaned text from URL:** User showed screenshot of UI with "Number of characters after cleaning: 0" for URL input. Raw text showed only `<html><head>` boilerplate (1389 chars, no body). Diagnosed as bot-blocking — `requests.get` with no headers gets minimal response from sites that detect bots. Fixed by adding browser User-Agent and 15s timeout to `load_url()`. User then clarified the actual case was authenticated internal pages, which can't be fixed at network level without session cookies. Agreed the right workaround is to download page as HTML and upload as file.

**Model discussion:** User asked "Which model is used by this code?" — local LLM is `bartowski/Qwen2.5-7B-Instruct-GGUF` (llama-cpp), TTS is `hexgrad/Kokoro-82M`. User asked where they are — checked HuggingFace cache, Kokoro already cached at `~/.cache/huggingface/hub/models--hexgrad--Kokoro-82M`, Qwen GGUF not downloaded yet (pulls on first use).

**Tampermonkey discussion:** User asked about using a Tampermonkey script to grab wiki content and pass to TTS. Three options presented: (A) Streamlit direct (no API, needs sidecar), (B) clipboard paste, (C) FastAPI sidecar with `/tts` endpoint. User asked what "sidecar" means — explained it's a companion process on same machine, different port. Decided on Option C: FastAPI on localhost:8080, Tampermonkey POSTs text, gets WAV back, plays inline on wiki page. Not yet implemented.

**Repo comparison:** User asked to compare alexmeckes/Document-To-Speech vs mozilla-ai/document-to-podcast. Agent fetched both. mozilla-ai is more mature (177 stars, CI, tests, modular architecture) and generates two-speaker podcast dialogue. alexmeckes is simpler single-voice. User decided to stick with alexmeckes — wants straight content reading, no podcast fluff.

**Fork:** User said they want to fork the repo to save code. Provided git commands to set new remote and push. Session ended with export-context request.

## Next Steps

1. User forks repo on GitHub and pushes current changes:
   ```bash
   cd /Users/bgandhi/Projects/Hackday/text-to-speech/Document-To-Speech
   git remote set-url origin https://github.com/<your-username>/Document-To-Speech.git
   git add src/document_to_speech/preprocessing/__init__.py \
           src/document_to_speech/config.py \
           demo/app.py \
           src/document_to_speech/preprocessing/data_loaders.py
   git commit -m "fix: url cleaner, openrouter_model optional, wav PCM_16, user-agent"
   git push
   ```
2. Build FastAPI sidecar (`demo/api.py`) with `POST /tts` endpoint that accepts `{"text": "..."}` and returns WAV bytes
3. Write Tampermonkey userscript that: extracts main article text from wiki page, strips nav/sidebar, POSTs to `localhost:8080/tts`, plays returned audio inline
4. Add CORS to FastAPI so browser can call localhost from any origin
5. Test end-to-end: Tampermonkey on a wiki page → FastAPI → Kokoro TTS → audio in browser

## How to Resume

Run `/import-context` in the new session to load this file and continue.
